[pytest]
markers =
    e2e: end-to-end frontend+backend test requiring browsers and dev servers
    smoke: marks tests as smoke tests (fast)
    slow: marks tests as slow (deselect with -m "not slow")